[project]
name = "cwd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dimitrisavellas", email = "dimitrisavellas8@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "graphviz>=0.21",
    "matplotlib>=3.11.2",
    "nbformat>=5.11.1",
    "numpy>=2.5.3",
    "pandas>=3.0.6",
    "seaborn>=0.13.2",
]

[project.scripts]
cwd = "cwd:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
